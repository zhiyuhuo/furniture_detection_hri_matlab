function res = kernel_rbf(u,v,rbfsigma)

if nargin < 3
    rbfsigma = 1;
end

kval = exp(-(1/(2*sigma^2))*(repmat(sqrt(sum(u.^2,2).^2),1,size(v,1))-2*(u*v')+repmat(sqrt(sum(v.^2,2)'.^2),size(u,1),1)));

end